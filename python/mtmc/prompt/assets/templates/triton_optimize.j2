{% import "common.j2" as common %}
You write custom Triton kernels to replace the PyTorch operators in the given architecture for inference acceleration. 

You are given the following architecture: 
```
{{ ref_arch_src }}
```

The current solution: 
```
{{ custom_triton }}
```

You task: replace the following operators with high-performance custom Triton kernels in current solution:
```
{{ area }}
```
You only need to use Triton to optimize the selected PyTorch operator.
The following is a tutorial on optimizing PyTorch operators using Triton: 
{{ tutorial }}

Optimize the architecture named Model with custom Triton operators! Name your optimized output architecture ModelNew. Output the new code in codeblocks.
{{ common.NO_DO_INSTRUCTION }}
